import { areas, site } from '@/data/content';
const slugify=(a:string)=>`${a.toLowerCase().replaceAll('ı','i').replaceAll('ö','o').replaceAll('ü','u').replaceAll('ş','s').replaceAll('ç','c').replaceAll('ğ','g')}-ozel-egitim-merkezi`;
export function generateStaticParams(){return areas.map(a=>({slug:slugify(a)}))}
export function generateMetadata({params}:{params:{slug:string}}){const area=areas.find(a=>slugify(a)===params.slug)||'Sincan';return {title:`${area} Özel Eğitim Merkezi`,description:`${area} ve çevresinde özel eğitim ve rehabilitasyon desteği için Altın Lale Özel Eğitim.`}}
export default function Page({params}:{params:{slug:string}}){const area=areas.find(a=>slugify(a)===params.slug)||'Sincan';return <main className="section"><div className="container"><span className="eyebrow">Bölgesel SEO Sayfası</span><h1 className="title" style={{marginTop:24}}>{area} Özel Eğitim Merkezi</h1><p className="subtitle">{area} ve çevresinde özel eğitim merkezi arayan aileler için Altın Lale Özel Eğitim ve Rehabilitasyon Merkezi, Sincan’daki merkezi konumuyla kolay ulaşılabilir, güven veren ve aileyle birlikte ilerleyen bir destek süreci sunar.</p><div className="cta" style={{marginTop:35}}><div><h2>Randevu ve bilgi için arayın</h2><p>{site.address}</p></div><a className="btn" href={site.phoneHref}>{site.phone}</a></div></div></main>}
