import Header from "@/components/Header";import Footer from "@/components/Footer";import CTA from "@/components/CTA";import { regions, services, site } from "@/data/content";import Link from "next/link";import type { Metadata } from "next";
const slugify=(s:string)=>s.toLowerCase().replaceAll(' ','-');
export function generateStaticParams(){return regions.map(r=>({slug:slugify(r)}))}
export function generateMetadata({params}:{params:{slug:string}}):Metadata{const r=regions.find(x=>slugify(x)===params.slug)||"Sincan";return {title:`${r} Özel Eğitim ve Rehabilitasyon Merkezi`,description:`${r} ve çevresinde özel eğitim, rehabilitasyon, bireysel eğitim, otizm, dil-konuşma ve öğrenme güçlüğü desteği.`}}
export default function Page({params}:{params:{slug:string}}){const r=regions.find(x=>slugify(x)===params.slug)||"Sincan";return <><Header/><main className="mx-auto max-w-6xl px-5 py-16"><p className="font-bold text-sage">Bölgesel SEO Sayfası</p><h1 className="mt-3 text-5xl font-black">{r} Özel Eğitim ve Rehabilitasyon Merkezi</h1><p className="mt-6 max-w-3xl text-xl leading-9 text-ink/70">{site.name}, {r} ve çevresinde özel eğitim desteği arayan aileler için Sincan’daki merkezi konumuyla ulaşılabilir, güven veren ve profesyonel bir destek süreci sunar.</p><div className="mt-10 grid gap-5 md:grid-cols-3">{services.map(s=><Link href={`/hizmetler/${s.slug}`} className="rounded-3xl bg-white p-6 shadow-soft" key={s.slug}><b>{s.title}</b><p className="mt-2 text-ink/60">{s.desc}</p></Link>)}</div><section className="mt-12 rounded-[2rem] bg-white p-8 shadow-soft"><h2 className="text-3xl font-black">{r} çevresinden gelen aileler için</h2><p className="mt-4 leading-8 text-ink/70">Kurumumuz Ankara Caddesi üzerinde yer aldığı için Sincan merkez, Eryaman, Etimesgut, Yenikent, Törekent ve Elvankent çevresinden kolay ulaşılabilir konumdadır. İlk görüşme için telefonla bilgi alabilir veya yol tarifi üzerinden merkezimize ulaşabilirsiniz.</p></section></main><CTA/><Footer/></>}
