import { blogs } from '@/data/content';
export function generateStaticParams(){return blogs.map((_,i)=>({slug:String(i+1)}))}
export function generateMetadata({params}:{params:{slug:string}}){const title=blogs[Number(params.slug)-1]||blogs[0];return {title,description:title}}
export default function Page({params}:{params:{slug:string}}){const title=blogs[Number(params.slug)-1]||blogs[0];return <main className="section"><article className="container" style={{maxWidth:900}}><a className="eyebrow" href="/">Aile Rehberi</a><h1 className="title" style={{marginTop:24}}>{title}</h1><p className="subtitle">Özel eğitim sürecinde her çocuğun gelişim yolculuğu farklıdır. Bu nedenle doğru merkez seçimi, aile ile kurum arasındaki iletişim, düzenli takip ve çocuğun ihtiyacına uygun hedeflerle ilerlemek büyük önem taşır.</p><p className="subtitle">Altın Lale Özel Eğitim ve Rehabilitasyon Merkezi, Sincan ve Ankara çevresindeki ailelere güven veren, anlaşılır ve sürdürülebilir bir özel eğitim yaklaşımı sunmayı hedefler.</p></article></main>}
