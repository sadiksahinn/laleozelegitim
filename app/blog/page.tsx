import Header from "@/components/Header";import Footer from "@/components/Footer";import { blogs } from "@/data/content";import Link from "next/link";
export default function Blog(){return <><Header/><main className="mx-auto max-w-6xl px-5 py-16"><h1 className="text-5xl font-black">Özel Eğitim Rehberi</h1><p className="mt-4 text-ink/70">Aileler için anlaşılır, güven veren ve SEO uyumlu rehber içerikler.</p><div className="mt-10 grid gap-5 md:grid-cols-2">{blogs.map(([slug,title])=><Link href={`/blog/${slug}`} className="rounded-3xl bg-white p-7 shadow-soft" key={slug}><b className="text-xl">{title}</b><p className="mt-3 text-ink/60">Altın Lale Özel Eğitim uzman yaklaşımıyla hazırlanmış bilgilendirici içerik.</p></Link>)}</div></main><Footer/></>}
